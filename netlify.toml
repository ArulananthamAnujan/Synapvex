[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — teacher share links like /school/:slug and /courses/:id
# are client-side routes and must serve index.html on direct hits.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
